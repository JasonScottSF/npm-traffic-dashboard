# Standalone health-check server — bypasses ModSecurity entirely.
# Mounted at /etc/nginx/conf.d/health.conf
server {
    listen 8081;
    access_log off;
    location /healthz {
        return 200 "OK\n";
        add_header Content-Type text/plain;
    }
}
